# HTML 动画转视频生成系统 - 作品说明

## 项目背景

本项目实现了一个基于 **AI 生成 HTML 页面，并自动转换为视频资源** 的系统。

用户通过 AI 生成包含动画效果的网页（HTML/CSS/JavaScript），系统自动运行网页、捕获动画过程，并生成可播放的视频文件，最终用于视频展示、内容生成、AI 创作等场景。

整体目标是实现：

> **HTML 动画 → 浏览器渲染 → 视频录制 → 视频资源管理**

类似于将网页作为一种新的动态视频创作格式。

---

# 技术方案选择

## 为什么不用 Canvas 直接生成视频？

Canvas 是一种优秀的图形渲染方案，也可以通过 `MediaRecorder API` 直接录制视频。

但是 Canvas 存在一定限制：

### 1. 无法完整支持 HTML 内容

现代网页不仅包含 Canvas 绘制内容，还包含：

* HTML DOM
* CSS 动画
* SVG
* Web Font
* 图片资源
* 视频元素
* 第三方 Web 组件

例如：

```html
<div class="title">
  Hello AI
</div>

<style>
.title {
  animation: fadeIn 2s;
}
</style>
```

这种基于 DOM + CSS 的动画无法直接转换到 Canvas。

如果要求 AI 生成任意网页，那么 Canvas 方案需要重新实现一套浏览器渲染能力，复杂度非常高。

### 2. 浏览器本身就是最好的渲染引擎

因此选择：

> 使用 Chromium 渲染 HTML，再通过录屏方式生成视频。

优势：

* 支持完整 Web 标准
* 支持 CSS Animation
* 支持 JavaScript 动画
* 支持 SVG/WebGL
* 与用户看到的网页效果保持一致

---

# 视频格式选择

## 为什么生成 MP4，而不是 WebM？

浏览器录制通常可以生成：

```
WebM + VP8/VP9
```

但是考虑后续视频分发需求，最终选择生成：

```
MP4 + H264
```

原因：

### 1. M3U8/HLS 兼容性

项目后续需要支持：

```
视频切片
+
HLS播放
+
CDN分发
```

即：

```
MP4
 ↓
ffmpeg
 ↓
HLS
 ↓
m3u8 + ts/fmp4
```

而传统 HLS 生态主要围绕：

* H264
* AAC
* MP4 Container

展开。

WebM 在 HLS 生态中支持较弱，尤其在移动端和传统播放器环境下兼容性不足。

### 2. 更好的生态支持

MP4/H264：

* 浏览器支持完善
* 手机端支持完善
* CDN支持成熟
* 视频平台兼容性高

因此选择：

```
HTML
 ↓
Chromium
 ↓
截图帧
 ↓
FFmpeg
 ↓
MP4(H264)
```

---

# 系统整体流程

## 业务流程

```
AI生成HTML网页
        |
        ↓
调用视频生成API
        |
        ↓
创建视频生成Job
        |
        ↓
任务队列
        |
        ↓
启动视频生成容器
        |
        ↓
Puppeteer启动Chromium
        |
        ↓
加载HTML页面
        |
        ↓
FFmpeg采集视频帧
        |
        ↓
生成MP4文件
        |
        ↓
上传对象存储
        |
        ↓
返回视频地址
```

---

# 系统架构

```
                 用户
                  |
                  |
             Web/API服务
                  |
                  |
          Job任务管理服务
                  |
                  |
          -----------------
          |               |
      Redis Queue      Database
          |
          |
   Video Worker Container
          |
          |
   ---------------------
   |                   |
Puppeteer          FFmpeg
   |
Chromium
   |
HTML/CSS/JS Renderer


          |
          ↓

      Object Storage
      (S3/OSS/MinIO)

          |
          ↓

        CDN
          |
          ↓

      视频播放
```

---

# 核心技术实现

## 1. Puppeteer网页渲染

使用 Chromium Headless：

负责：

* 加载 HTML
* 执行动画
* 等待页面稳定
* 控制浏览器生命周期

例如：

```javascript
await page.goto(url);

await page.waitForTimeout(duration);
```

确保 CSS/JS 动画正常执行。

---

## 2. FFmpeg视频生成

通过 FFmpeg 完成：

* 视频编码
* 帧率控制
* 分辨率设置
* H264压缩

生成：

```
frame_00001.png
frame_00002.png
frame_00003.png
...
        |
        ↓
    FFmpeg
        |
        ↓
    output.mp4
```

---

# 容器化设计

视频生成环境独立运行：

```
video-worker-container
```

内部包含：

* Chromium
* Puppeteer
* FFmpeg

优势：

### 环境隔离

避免：

* 浏览器版本冲突
* FFmpeg依赖冲突
* Node环境污染

### 弹性扩展

未来可以：

```
Job Queue

      |
 ----------------
 |      |       |
Worker Worker Worker
```

根据任务量动态扩容。

---

# 后续优化方向

## 1. 视频生成服务独立化

当前：

```
API
 |
生成视频
```

优化：

```
API Service

      |
      ↓

Video Generation Service

      |
      ↓

Worker Cluster
```

优点：

* 独立扩容
* 异步处理
* 提高稳定性

---

## 2. 支持录制指定区域

当前：

```
整个浏览器窗口
```

优化：

支持：

```json
{
  "selector": "#animation",
  "width":800,
  "height":600
}
```

只录制指定 DOM：

例如：

* 海报区域
* PPT页面
* AI生成动画区域

---

## 3. 可配置视频参数

支持用户指定：

### 分辨率

```
1920x1080
1280x720
1080x1080
```

### FPS

```
24
30
60
```

### 时长

```
5s
10s
30s
```

### 编码参数

```
H264
H265
AV1
```

---

## 4. 使用浏览器原生捕获优化性能

目前：

```
网页
 ↓
截图
 ↓
FFmpeg编码
```

后续可以研究：

```
Chromium
 ↓
PipeWire / CDP Capture
 ↓
FFmpeg
```

减少中间图片帧，提高：

* 速度
* CPU利用率
* 长视频稳定性

---

## 5. 增加任务状态管理

支持：

```
CREATED

 ↓

RUNNING

 ↓

PROCESSING

 ↓

SUCCESS

 ↓

FAILED
```

并提供：

* 进度查询
* 失败重试
* 日志查看

---

# 项目亮点总结

1. **采用浏览器级渲染方案，兼容完整 HTML/CSS/JS 动画生态**
2. **通过容器化视频 Worker 实现任务隔离和水平扩展**
3. **使用 FFmpeg 输出 MP4，兼顾播放兼容性和后续 HLS 分发**
4. **设计 Job 异步任务模型，适合 AI 内容生成场景扩展**
5. **整体架构可演进为 AI 视频生成基础设施**

---
